import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.muhdfaris.fntrack.admin',
  appName: 'fntrack-admin',
  webDir: 'www'
};

export default config;
